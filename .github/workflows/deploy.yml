name: Deploy to Production

on:
  push:
    branches:
      - production

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Set up SSH key
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H $SERVER_IP >> ~/.ssh/known_hosts
          echo "SSH_PRIVATE_KEY=${{ secrets.SSH_PRIVATE_KEY }}"  # Debug: Print SSH key variable
          echo "SERVER_IP=${{ secrets.SERVER_IP }}"  # Debug: Print Server IP variable
          echo "SERVER_USER=${{ secrets.SERVER_USER }}" 
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          SERVER_IP: ${{ secrets.SERVER_IP }}
          SERVER_USER: ${{ secrets.SERVER_USER }}

      - name: Deploy to Server
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          SERVER_IP: ${{ secrets.SERVER_IP }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
        run: |
          ssh -o StrictHostKeyChecking=no -p 4545 $SERVER_USER@$SERVER_IP "
            cd /home/new10s/newTens2.0/client &&
            git pull origin production &&
            npm install &&
            npm run build &&
            cd ../server &&
            npm install &&
            pm2 restart all &&
            sudo systemctl reload nginx
          "
